@*@model IEnumerable<Currency>*@
<script src="~/js/webConnect.js"></script>
<div class="bg-primary m-1 p-1 text-white"><h4>Curencies</h4></div>
<div class="container-fluid">
    <div class="row">
        <div class="col-xs-4 col-sm-4 col-md-4 col-lg-4">
            <table class="table table-sm table-bordered">
                <tr><th>Currency</th><th>Unit</th><th>Value</th><th>Actions</th></tr>
                @if (Model.Count() == 0)
                {
                    <tr><td colspan="3" class="text-center">Brak danych - jesteś offline.</td></tr>
                }
                else
                {
                    @*foreach (Currency currency in Model)
                    {
                        <tr>
                            <td>@currency.CurrencyName</td>
                            <td>@currency.Unit</td>
                            <td>@currency.Price</td>
                            <td>
                                <form asp-action="Buy" method="post">
                                    <button type="submit" class="btn btn-sm btn-danger"> Buy  </button>
                                </form>
                            </td>
                        </tr>
                    }*@
                    <script>
                        jQuery.each(dataJsonFromWeb, function (
                            $("#" + this).text("tw" + this + ".");
                        ) { });
                    </script>
                }
            </table>
        </div>
        <div class="col-xs-4 col-sm-4 col-md-4 col-lg-4">
            <table class="table table-sm table-bordered">
                <tr><th>Currency</th><th>Unit</th><th>Value</th><th>Actions</th></tr>
                @if (Model.Count() == 0)
                {
                    <tr><td colspan="3" class="text-center">Brak danych - jesteś offline.</td></tr>
                }
                else
                {
                    @*foreach (Currency currency in Model)
                    {
                        <tr>
                            <td>@currency.CurrencyName</td>
                            <td>@currency.Unit</td>
                            <td>@currency.Price</td>
                            <td>
                                <form asp-action="Buy" method="post">
                                    <button type="submit" class="btn btn-sm btn-danger"> Buy  </button>
                                </form>
                            </td>
                        </tr>
                    }*@
                }
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-xs-4 col-sm-4 col-md-4 col-lg-4">
        </div>
        <div class="col-xs-4 col-sm-4 col-md-4 col-lg-4">
            Test
        </div>
    </div>
</div>